<div class="container">
  <h2 class="page-title">グループ参加</h2>
  <div class="mt-3">グループ名：<%= @group.name %></div>

  <div class="row">
    <div class="mx-auto mt-5">
      <p><strong>グループのパスワードをご入力ください。</strong></p>
      <p>
        ※本パスワードは、グループ作成時に設定いただいたものです。<br>
      　ご不明な場合は、グループ作成者へお伺いください。
      </p>

      <%= form_with model: @group, url: join_create_group_path, method: :post, local: true do |f| %>
        <p id="alert"><%= alert %></p>
        <%= f.label :password, "パスワード", class: "mt-3 mr-3" %>
        <%= f.password_field :password, class: "mt-3 p-1" %>
        <p class="offset-2"><%= f.submit "参加する", class: 'btn btn-orange mt-3 px-5' %></p>
      <% end %>
    </div>
  </div>
</div>
