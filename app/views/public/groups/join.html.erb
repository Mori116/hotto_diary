<div class="container">
  <h2 class="page-title">グループ参加</h2>
  <div class="mt-3">グループ名：<%= @group.name %></div>

    <div class="row">
      <div class="mx-auto mt-5">
        <p class="message-title">グループのパスワードをご入力ください。</p>
      </div>
    </div>
    <div class="row">
      <p class="mx-auto">
        ※本パスワードは、グループ作成時に設定いただいたものです。<br>
      　ご不明な場合は、グループ作成者へお伺いください。
      </p>
    </div>

    <%= form_with model: @group, url: join_create_group_path, method: :post, local: true do |f| %>
      <div class="row">
        <div class="mx-auto">
          <p id="alert"><%= alert %></p>
          <%= f.label :password, "パスワード", class: "mr-3" %>
          <%= f.password_field :password, class: "p-1" %>
        </div>
      </div>

      <div class="row">
        <div class="mx-auto">
          <%= f.submit "参加する", class: 'btn btn-orange mt-3 px-5' %>
        </div>
      </div>
    <% end %>

</div>
